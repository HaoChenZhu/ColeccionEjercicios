#include <iostream>
using namespace std;
int main() {
  string text{" "};
  char a{'a'};
  int cont=0;
  cout<<"Introduce un texto"<<endl;
  getline(cin,text);
  for(int i{0}; i<text.size();i++){
    if(text[i]==a){
      cont++;
    }
  }
  cout<<cont;
}
