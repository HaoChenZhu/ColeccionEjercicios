#include <iostream>
#include<vector>
using namespace std;

int main(){
  int a,b,c;
  vector<int>vectorcito;
  cout<<"Introduce 3 numeros"<<endl;
  cin>>a>>b>>c;
  if(c<a || a>>c){
    cout<<"Fuera de rango";
    return 0;
  }
  for(int i=a; i<=b;i++){
    if(i%c==0)vectorcito.push_back(i);
  }
  for(auto e:vectorcito){
    cout<<e<<" ";
  }
}
